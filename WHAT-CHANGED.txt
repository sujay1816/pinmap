Pinmap — fonts and buttons (build 2026-08-03.23)
================================================

Changed since build .22, which is what is on GitHub now.

  index.html              stylesheet only — 21 lines, all CSS or comments.
                          No script, no markup, no logic touched.
  tests/ui/14-audit.mjs   eight checks so none of it can regress

Copy over the same paths, delete this file and CHANGES.diff, then:
  npm test
Expect 449 checks, all passing.
