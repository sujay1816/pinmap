Pinmap — Sri Tex clutch end, and the mirrored body file
=======================================================

Copy these over the same paths in your clone, keeping the folder
structure, then commit in GitHub Desktop. Delete this file and
CHANGES.diff first — neither belongs in the repository.

  index.html               the app
  README.md                both changes written up
  tests/extract.mjs        exports the company table to the suites
  tests/ui/16-company.mjs  updated: Sri Tex now mirrors, and brings a clutch
  tests/ui/17-clutch.mjs   NEW: the clutch end
  tests/logic/22.mjs       updated: the stale note about mirroring
  tests/logic/24.mjs       NEW: holds the body file against their real one

After copying, run:  npm test
Expect 449 checks, 24 logic suites and 17 interface suites, all passing.

THE ONE THING TO CHECK ON THE LOOM
----------------------------------
The body file now comes out back to front against the border file,
which is what Sri Tex's own body file does. Build one job you already
have a known-good file for and run it through "Check against a file you
trust" before weaving anything real.
