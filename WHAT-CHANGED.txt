Pinmap — build 2026-08-03.35
============================

This zip holds only what changed since the recharge zip. If you have not
applied the earlier ones, CHANGES.diff covers everything since build .22
on GitHub.

  index.html              21 browser dialogs replaced with the app's own;
                          Choose File buttons styled
  README.md               written up
  tests/extract.mjs       unchanged export list
  tests/ui/14-audit.mjs   fails if a native dialog creeps back
  tests/ui/15-verify.mjs  answers the dialog rather than stubbing confirm
  tests/ui/23-credits.mjs drives the recharge dialog as a weaver would

  npm test  ->  473 checks, all passing
