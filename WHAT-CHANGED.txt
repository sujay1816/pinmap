Pinmap — build 2026-08-03.34
============================

This zip holds only what changed since the credits zip. If you have not
applied that one yet, use CHANGES.diff, which covers everything since
build .22 on GitHub.

  index.html              every recharge code is now a different string
  README.md               what the codes can and cannot do
  tools-codes.mjs         NEW: print a batch of codes
  tests/extract.mjs       exports makeCodes
  tests/ui/23-credits.mjs mints its own code rather than using a fixed one

  npm test  ->  473 checks, all passing

PRINT CODES
-----------
  node tools-codes.mjs 50 20     twenty codes worth 50 credits each
