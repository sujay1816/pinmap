Only in ../verify: CHANGES.diff
diff -ru -x node_modules -x .git -x core.mjs ../verify/README.md ../pinmap/README.md
--- ../verify/README.md	2026-08-03 11:34:51.543374973 +0000
+++ ../pinmap/README.md	2026-08-04 06:22:33.109208527 +0000
@@ -171,6 +171,37 @@
 border file, the body and locking pins stay down in it, and the achu is half up
 and half down, flipping every line.
 
+### How Sri Tex work the box
+
+Their box is four pins, so a half is a pair. Which half lifts depends on how
+many designs are loaded:
+
+| Designs | 1st | 2nd | 3rd |
+| --- | --- | --- | --- |
+| one | first half | | |
+| two | second half | first half | |
+| three | second half | both down | first half |
+
+Four is deliberately left out. Nobody has said what it should be, so it falls
+back to the standard table and can be set by hand per weft.
+
+Two things make this different from the standard table. The question is asked
+**of each weft separately** — not "is there a figure anywhere on this line" but
+"is there anything to weave on this weft". And a weft with nothing to weave
+does **not** drop its box pins: it takes the one-design setting — the first
+half — and the code reads that from the table rather than naming a half of its
+own, so the two cannot drift apart.
+
+The loom the samples came off, read from the right, is:
+
+    achu 10 | box 4 | hit 2 | empty 2 | border 375
+            | locking 16 + body 720 + locking 16
+            | border 375 | achu 14 | hit 2 | empty 32     = 1568
+
+That order was settled against their own two files: every group in it is either
+wholly lifted or wholly down in each file, which no other arrangement managed.
+The achu appears only in the border file, the box only in the body file.
+
 ### Why the Sri Tex body file is turned round
 
 This was wrong for a long time and is worth writing down, because a mirrored
@@ -204,18 +235,38 @@
 those settings as a new entry in the `COMPANIES` table near the top of the
 script.
 
+### Choosing a weave
+
+Two groups are worked from a weave, and they differ only in where the weave
+comes from:
+
+- **Locking** — the built-in satins and twills
+- **Weave library** — whatever has been uploaded to the library
+
+Pick the group in the Group column and its weaves are in the Content column
+beside it. Nothing else changes between them; they are composed identically.
+
+They are separate groups on purpose. An uploaded weave used to be a heading
+part way down the Locking row's list of satins, where somebody who had just
+uploaded one would never think to look. A Weave library group with nothing
+uploaded says so in its own list rather than sitting empty and unexplained.
+
+Choosing a weave sets the group's pin count to the weave's width.
+
 ### The clutch end
 
 A Sri Tex loom carries its clutch at one end, and the thirty-two pins sitting
 over it cannot take a design. Registering a loom as Sri Tex therefore asks one
 more question — **left clutch or right clutch** — and puts an empty group of
-thirty-two pins at that end. Nothing else asks it, because nothing else needs
-to; the field is not shown for any other company.
+thirty-two pins **at the left of the board**, whichever end the clutch is on.
+The end is a fact about the loom and worth recording, but it does not decide
+where the pins sit: on a Sri Tex loom they are the first thirty-two either way.
+Nothing else asks the question, because nothing else needs to; the field is not
+shown for any other company.
 
 After that it is an ordinary group. Drag it somewhere else, change its count,
-or take it out entirely — nothing reaches back in and corrects it. The only
-thing that moves it is changing the clutch end itself, and that carries your
-count across rather than resetting it.
+or take it out entirely — nothing reaches back in and corrects it. Changing the
+clutch end keeps a count you set yourself rather than resetting it.
 
 Changing the loom to a company with no clutch takes the group away again, but
 only while it is still thirty-two pins. A count you set yourself is yours, so
@@ -272,6 +323,82 @@
 | What | Kept in |
 | --- | --- |
 | Looms, weaves, box settings, session | Browser storage, scoped per account |
+
+### Asking, in the app's own voice
+
+There are no `alert`, `confirm` or `prompt` calls left. A browser's own dialog
+prints *"x.test says"* above the words, cannot be styled at all, and is the
+first thing that makes a page look like a page rather than a tool.
+
+Three helpers replace them, all returning a promise so a caller reads much as
+it did with an `await` in front:
+
+- `say(body, title)` — one button, for something that has gone wrong
+- `ask(body, opts)` — two buttons, for something that cannot be undone
+- `askText(body, opts)` — a box to type in
+
+Escape and the backdrop cancel, Enter confirms, and focus goes back to whatever
+opened the dialog. `tests/ui/14-audit.mjs` fails if a native dialog creeps back.
+
+The browser's **Choose File** button is styled the same way, since it is grey
+operating-system chrome otherwise and looks different in every browser.
+
+### Credits
+
+A credit is spent when a **finished file is downloaded** — the border file and
+the body file are one each. Building, previewing and checking against a file you
+trust are all free, on purpose: those are the steps that catch a wrong pin map,
+and charging for them would teach people to skip the one thing that saves the
+silk. A new account starts with `CREDITS_ON_JOINING`.
+
+Two numbers are kept rather than one, and both only ever climb: `granted` and
+`spent`. Merging two machines takes the larger of each, so neither a spend nor a
+grant can be lost when the same account is used in two places.
+
+**This is a meter, not a lock.** Everything runs in the weaver's own browser, so
+the numbers are theirs to edit, and the page works offline with no network at
+all. It counts honest use and shows what is left. Making it binding would mean
+the conversion itself happening somewhere the weaver does not control — a
+different shape of product, and one that gives up working offline. The code is
+arranged so that day replaces one function, `spendCredits`.
+
+#### Recharge codes
+
+A stand-in until payment is wired up, so accounts can be topped up by hand.
+A code is `PIN-<credits>-<serial>-<check>`, where the check is made from the
+amount, the serial and `CODE_SALT`.
+
+Print a batch with:
+
+    node tools-codes.mjs 50 20      # twenty codes worth 50 credits each
+
+Every code is a different string. That matters: one universal code for "50
+credits" is a password for free credits, not a recharge. Give each customer
+their own and keep a note of who got which, so a leak can be traced.
+
+**What it can and cannot do.** The check stops a mistyped code and stops the
+same code being redeemed twice on one account. It cannot stop a code being used
+on a *second* account, and it cannot stop anyone reading this page from writing
+their own — there is no server to record what has been spent. Real one-time
+codes need the redemption recorded somewhere the weaver does not control.
+
+Change `CODE_SALT` and every code issued before it stops working.
+
+### Staying signed in
+
+Firebase keeps the sign-in in the browser it was made in. The app used not to
+ask it for one: nothing listened for the kept session, so every reload woke with
+no user and went straight back to Google for a fresh token — on a machine that
+had never signed out. If the One Tap prompt was blocked, or had been dismissed
+often enough for Google to back off, the account simply stayed disconnected.
+
+Now the session is asked for first. Waking calls `restoreCloud`, which starts
+Firebase, waits for `onAuthStateChanged` to say whether a sign-in was kept, and
+syncs straight away if one was. Google is only troubled when there is genuinely
+no session. A session that later runs out says so on the dot rather than going
+quiet, and signing out ends the Firebase session as well — otherwise the next
+restore would walk straight back into the account just left.
+
 | The same, shared between machines | Firestore, if configured — see above |
 | Backups | `pinmap_backup_YYYY-MM-DD.json`, written by *Back up everything* |
 | Uploaded design files | Not kept — reselect them after a reload |
Only in ../verify: WHAT-CHANGED.txt
diff -ru -x node_modules -x .git -x core.mjs ../verify/index.html ../pinmap/index.html
--- ../verify/index.html	2026-08-03 11:34:51.543374973 +0000
+++ ../pinmap/index.html	2026-08-04 06:19:03.584793953 +0000
@@ -47,6 +47,7 @@
     --c-body:   #52737E;
     --c-empty:  #B9C3C8;
     --c-lock:   #7A4E86;
+    --c-weavelib: #9A5E52;
     --mono: ui-monospace, "SF Mono", SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace;
     --sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
 
@@ -59,6 +60,7 @@
     --t-body:  15.5px;   /* prose */
     --t-field: 15px;     /* what you type into */
     --t-fig:   17px;     /* numbers worth reading across a room */
+    --t-head:  18.5px;   /* the title of a screen */
     --track:   0.09em;   /* uppercase tracking, tighter than before */
   }
   * { box-sizing: border-box; }
@@ -69,6 +71,7 @@
     -webkit-font-smoothing: antialiased;
   }
   .wrap { max-width: 1060px; margin: 0 auto; padding: 0 20px 96px; }
+  p, .fine, .tagline, .hint, .fieldnote, .wl-note { text-wrap: pretty; }
 
   header { border-bottom: 2px solid var(--ink); padding: 20px 0 0; margin-bottom: 26px; }
   .brandline { display: flex; align-items: flex-end; gap: 18px; flex-wrap: wrap; }
@@ -101,12 +104,16 @@
 
   .screen[hidden] { display: none; }
   .head-row { display: flex; align-items: baseline; gap: 12px; flex-wrap: wrap; border-bottom: 1px solid var(--rule); padding-bottom: 8px; margin-bottom: 18px; }
-  .head-row h2 { font-size: 17px; font-weight: 650; margin: 0; letter-spacing: -0.01em; }
+  /* The screen title used to sit 1.5px above body text, which at arm's length
+     is no hierarchy at all. Far enough above the prose now to be found without
+     crowding the wordmark. text-wrap keeps a title off a lonely second line. */
+  .head-row h2 { font-size: var(--t-head); font-weight: 650; margin: 0; letter-spacing: -0.01em; text-wrap: balance; }
   .head-row .hint { margin-left: auto; font-size: var(--t-small); color: var(--ink-3); }
 
   label.field { display: block; }
   label.field[hidden] { display: none; }
   .fieldnote { display: block; margin-top: 5px; font-size: var(--t-small); color: var(--ink-3); line-height: 1.4; }
+  .pvnote { font-family: var(--mono); font-size: var(--t-small); color: var(--ink-3); margin-top: 7px; }
   label.field > .cap, .cap {
     display: block; font-family: var(--mono); font-size: var(--t-small); letter-spacing: var(--track);
     text-transform: uppercase; color: var(--ink-3); margin-bottom: 5px;
@@ -121,14 +128,24 @@
   button {
     font-family: var(--mono); font-size: var(--t-small); letter-spacing: var(--track); text-transform: uppercase;
     background: #fff; color: var(--ink); border: 1px solid var(--ink); padding: 8px 13px; cursor: pointer; border-radius: 0;
+    /* A press should be felt. Without this a button gave nothing back until the
+       screen redrew, which on a slow shed PC reads as a click that missed. */
+    transition: background-color .12s ease, color .12s ease, border-color .12s ease, transform .06s ease;
+    -webkit-tap-highlight-color: transparent;   /* the grey flash, replaced by :active below */
+    user-select: none;                          /* a double click should not select the label */
   }
   button:hover:not(:disabled) { background: var(--ink); color: #fff; }
-  button:disabled { opacity: 0.35; cursor: not-allowed; }
+  button:active:not(:disabled) { transform: translateY(1px); }
+  button:disabled { opacity: 0.42; cursor: not-allowed; }
   button.ghost { border-color: var(--rule); color: var(--ink-2); }
   button.ghost:hover:not(:disabled) { background: var(--ink-2); border-color: var(--ink-2); color: #fff; }
   button.tiny { padding: 4px 8px; font-size: var(--t-small); letter-spacing: 0.05em; }
   button.solid { background: var(--ink); color: #fff; }
   button.solid:hover:not(:disabled) { background: #000; }
+  /* A button that destroys something should say so before it is hovered, not
+     after. Restrained at rest — the word in red, the frame left alone — and
+     unmistakable once it is under the pointer. */
+  button.danger { color: var(--meena); }
   button.danger:hover:not(:disabled) { background: var(--meena); border-color: var(--meena); color: #fff; }
   .btn-row { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 16px; }
   .spacer { margin-left: auto; }
@@ -293,7 +310,7 @@
   }
   .ruler-seg .c { font-family: var(--mono); font-size: var(--t-small); font-weight: 700; color: #fff; font-variant-numeric: tabular-nums; }
   .k-empty .k, .k-empty .c, .k-none .k, .k-none .c { color: var(--ink-2); }
-  .k-box .c, .k-leftBorder .c, .k-rightBorder .c, .k-achu .c, .k-body .c, .k-locking .c { color: #fff; }
+  .k-box .c, .k-leftBorder .c, .k-rightBorder .c, .k-achu .c, .k-body .c, .k-locking .c, .k-weaveLib .c { color: #fff; }
   .k-empty .c { color: var(--ink-2); }
   .k-box { background: var(--c-box); }
   .k-leftBorder { background: var(--c-lborder); }
@@ -302,6 +319,7 @@
   .k-body { background: var(--c-body); }
   .k-empty { background: var(--c-empty); }
   .k-locking { background: var(--c-lock); }
+  .k-weaveLib { background: var(--c-weavelib); }
   .k-none { background: #fff; }
 
   /* ---------- layers ---------- */
@@ -386,6 +404,31 @@
   .whoami { display: flex; align-items: center; gap: 8px; }
   .whoami .nm2 { font-family: var(--mono); font-size: var(--t-small); color: var(--ink-2); }
   .whoami img { width: 20px; height: 20px; border-radius: 50%; }
+  /* A question the app asks, in its own voice rather than the browser's. The
+     grey box a browser puts up prints "x.test says" above the words and cannot
+     be styled at all, which is the first thing that makes a page look like a
+     page rather than a tool. */
+  .ask { position: fixed; inset: 0; background: rgba(20,24,28,0.55); z-index: 60;
+    display: flex; align-items: center; justify-content: center; padding: 24px; }
+  .ask[hidden] { display: none; }
+  .ask-box { background: var(--paper); border: 1px solid var(--ink); max-width: 460px; width: 100%;
+    padding: 22px 24px 18px; box-shadow: 0 18px 48px rgba(20,24,28,0.28); }
+  .ask-box h4 { font-family: var(--mono); font-size: var(--t-small); letter-spacing: var(--track);
+    text-transform: uppercase; color: var(--ink-3); margin: 0 0 10px; }
+  .ask-box p { margin: 0 0 16px; line-height: 1.55; white-space: pre-line; }
+  .ask-box input[type=text] { width: 100%; margin-bottom: 16px; }
+  .ask-acts { display: flex; gap: 8px; justify-content: flex-end; }
+
+  /* The browser's own "Choose File" button is grey OS chrome and looks
+     different in every browser. Make it one of ours. */
+  input[type=file]::file-selector-button {
+    font-family: var(--mono); font-size: var(--t-small); letter-spacing: var(--track);
+    text-transform: uppercase; background: #fff; color: var(--ink); border: 1px solid var(--rule);
+    padding: 6px 11px; margin-right: 10px; cursor: pointer; border-radius: 0;
+    transition: background-color .12s ease, color .12s ease, border-color .12s ease;
+  }
+  input[type=file]:hover::file-selector-button { background: var(--ink-2); border-color: var(--ink-2); color: #fff; }
+
   .viewer {
     position: fixed; inset: 0; background: rgba(20,24,28,0.82); z-index: 60;
     display: flex; flex-direction: column; padding: 18px;
@@ -411,6 +454,13 @@
     justify-content: flex-end; padding-bottom: 2px;
   }
   .savestate i { width: 6px; height: 6px; border-radius: 50%; background: var(--ok); display: inline-block; }
+  .credits {
+    font-family: var(--mono); font-size: var(--t-small); letter-spacing: var(--track); text-transform: uppercase;
+    color: var(--ink-3); display: flex; align-items: center; gap: 8px; flex-basis: 100%; justify-content: flex-end;
+  }
+  .credits b { font-weight: 700; color: var(--ink); }
+  .credits.low b { color: var(--meena); }
+  .credits.out { color: var(--meena); }
   .syncstate {
     font-family: var(--mono); font-size: var(--t-small); letter-spacing: var(--track); text-transform: uppercase;
     color: var(--ink-3); display: flex; align-items: center; gap: 6px; flex-basis: 100%;
@@ -512,6 +562,8 @@
   .wl-item .sub { font-family: var(--mono); font-size: var(--t-small); color: var(--ink-3); margin-top: 3px; }
   .wl-empty { font-size: var(--t-note); color: var(--ink-3); }
   .weave-cell { display: flex; align-items: center; gap: 7px; }
+  .weave-picks { display: flex; flex-direction: column; gap: 5px; flex: 1; min-width: 0; }
+  .weave-picks select { width: 100%; }
   .weave-cell canvas { width: 22px; height: 22px; image-rendering: pixelated; border: 1px solid var(--rule); flex: 0 0 22px; }
   .content-note { font-family: var(--mono); font-size: var(--t-small); color: var(--ink-3); }
   h3.sec {
@@ -608,6 +660,8 @@
      pin count are 26px, and a mis-hit there changes the pin map. A finger wants
      nearer 44px, so give it that without moving anything on a mouse. */
   @media (pointer: coarse) {
+    /* Anything under 16px makes iOS Safari zoom the page on focus. */
+    input[type=text], input[type=number], input[type=search], select, textarea { font-size: 16px; }
     button.tiny { padding: 10px 14px; }
     .stepper { width: 42px; flex: 0 0 42px; min-height: 42px; }
     .grip { padding: 11px 10px; }
@@ -620,6 +674,18 @@
 
 <div id="toast" role="status" aria-live="polite"></div>
 
+<div class="ask" id="ask" hidden role="dialog" aria-modal="true" aria-labelledby="askTitle">
+  <div class="ask-box">
+    <h4 id="askTitle"></h4>
+    <p id="askBody"></p>
+    <input type="text" id="askInput" hidden autocomplete="off" spellcheck="false">
+    <div class="ask-acts">
+      <button class="ghost" id="askNo" hidden>Cancel</button>
+      <button class="solid" id="askYes">OK</button>
+    </div>
+  </div>
+</div>
+
 <div class="viewer" id="viewer" hidden role="dialog" aria-modal="true" aria-label="Design viewer">
   <div class="viewer-bar">
     <span class="viewer-title" id="viewerTitle"></span>
@@ -692,6 +758,7 @@
       <div class="savestate" id="saveState" aria-live="polite"></div>
       <div class="whoami" id="whoami" style="flex-basis:100%;justify-content:flex-end">
       </div>
+      <div class="credits" id="credits" aria-live="polite"></div>
       <div class="syncstate" id="syncState" aria-live="polite"></div>
       <div class="syncwhy" id="syncWhy" hidden></div>
       <div id="syncBtnSlot" hidden></div>
@@ -791,7 +858,7 @@
     <div class="weavelib">
       <div class="wl-head">
         <span class="cap" style="margin:0">Weave library</span>
-        <span class="wl-note">A named BMP here stays available to every loom.</span>
+        <span class="wl-note">A named BMP here can then be chosen on any <strong>Weave library</strong> group, on this loom or any other.</span>
       </div>
       <div class="wl-add">
         <input type="text" id="weaveName" placeholder="name it, e.g. Murthy lock 8">
@@ -806,6 +873,7 @@
       <button data-add="box">+ Box</button>
       <button data-add="leftBorder">+ Left border</button>
       <button data-add="locking">+ Locking</button>
+      <button data-add="weaveLib">+ Weave library</button>
       <button data-add="body">+ Body</button>
       <button data-add="rightBorder">+ Right border</button>
       <button data-add="empty">+ Empty</button>
@@ -872,9 +940,12 @@
           <button class="tiny ghost" data-bzoomv="border" data-zoom="fit">Fit</button>
           <button class="tiny ghost" data-bzoomv="border" data-zoom="1">1:1</button>
           <button class="tiny ghost" data-bzoomv="border" data-zoom="3">3×</button>
+          <button class="tiny ghost" id="borderPvMode" aria-pressed="true">Colour groups</button>
         </div>
       </div>
       <div class="pv-frame"><canvas id="borderPv" role="img" aria-label="Preview of the border file"></canvas></div>
+      <div class="legend" id="borderPvLegend"></div>
+      <div class="pvnote" id="borderPvNote"></div>
     </div>
     <details class="note">
       <summary>What goes in this file</summary>
@@ -963,10 +1034,17 @@
   box:         { label: "Box",          cls: "k-box",         colour: "#2E3A45", code: 1, source: "generated" },
   leftBorder:  { label: "Left border",  cls: "k-leftBorder",  colour: "#1F4E6B", code: 2, source: "border"    },
   locking:     { label: "Locking",      cls: "k-locking",     colour: "#7A4E86", code: 5, source: "generated" },
+  weaveLib:    { label: "Weave library", cls: "k-weaveLib",    colour: "#9A5E52", code: 9, source: "generated" },
   body:        { label: "Body",         cls: "k-body",        colour: "#52737E", code: 6, source: "weft"      },
   rightBorder: { label: "Right border", cls: "k-rightBorder", colour: "#3E7A8F", code: 3, source: "border"    },
   empty:       { label: "Empty",        cls: "k-empty",       colour: "#B9C3C8", code: 7, source: "down"      }
 };
+// Locking works a built-in satin or twill; Weave library works an uploaded one.
+// They behave the same in every other way, so ask this rather than naming
+// either of them.
+const WEAVE_KINDS = ["locking", "weaveLib"];
+const usesWeave = (k) => WEAVE_KINDS.indexOf(k) >= 0;
+
 const WEFT_SLOTS = 4;                 // how many slots you start with
 const MAX_WEFTS = 8;                  // and how many you may add up to
 const WEFT_NAMES = ["Rani", "Zari", "Meena 1", "Meena 2"];
@@ -984,7 +1062,7 @@
   for (let j = 0; j <= i; j++) if (isOne(segs[j])) n++;
   return `${base} ${n}`;
 }
-const BUILD = "2026-08-03.22";      // bump when the file changes, so a stale page shows
+const BUILD = "2026-08-03.35";      // bump when the file changes, so a stale page shows
 /* ============================================================
    Whose software wrote the file
 
@@ -1043,7 +1121,7 @@
   pinOneLeft: true, topRowFirstPick: true, blackIsIndexZero: true,
   achuStartsBlack: true, stackMode: "interleave", weavePerDesignLine: true,
   satinWarpFaced: false, boxWholeBand: true, autoRotate: true,
-  mirrorBodyFile: false, bodyGround: ""
+  mirrorBodyFile: false, bodyGround: "", boxScheme: "default"
 };
 
 const COMPANIES = [
@@ -1051,6 +1129,7 @@
   { id: "saitex", name: "Sai Tex", opts: { ...COMPANY_BASE } },
   { id: "sritex", name: "Sri Tex", opts: { ...COMPANY_BASE,
       blackIsIndexZero: false,     // white is the lifted pin
+      boxScheme: "sritex",         // their own way of working the box
       mirrorBodyFile: true,        // their body file is written back to front
       bodyGround: "satin:2:1" } }  // and a plain ground under every weft
 ];
@@ -1140,7 +1219,8 @@
     boxWholeBand: true,
     autoRotate: true,
     weavePerDesignLine: true,
-    satinWarpFaced: false
+    satinWarpFaced: false,
+    boxScheme: "default"
   },
   built: null,
   builtBorder: null,
@@ -1482,7 +1562,35 @@
   }
 }
 
-function boxPlan(weftCount, weftIndex, figureOnLine) {
+/* ------------------------------------------------------------
+   Sri Tex work their box differently
+
+   Their box is four pins, so a half is a pair. Which half lifts depends
+   on how many designs are loaded, and it is decided per line:
+
+       one design      the first half
+       two designs     the second half, then the first half
+       three designs   second half, both pins down, then the first half
+
+   Four is left out on purpose. Nobody has said what it should be, so it
+   falls back to the standard table and can be set by hand per weft.
+
+   And a line with nothing to weave on that weft does not leave the box
+   down — it takes the one-design setting, whatever that is.
+   ------------------------------------------------------------ */
+const SRITEX_BOX = {
+  1: ["first"],
+  2: ["second", "first"],
+  3: ["second", "none", "first"]
+};
+
+function boxPlan(weftCount, weftIndex, figureOnLine, scheme) {
+  if (scheme === "sritex") {
+    const row = SRITEX_BOX[weftCount];
+    if (!row) return boxPlan(weftCount, weftIndex, figureOnLine);   // four wefts, or more
+    if (!figureOnLine) return SRITEX_BOX[1][0];   // nothing to weave: the one-design setting
+    return row[weftIndex] || "none";
+  }
   if (weftCount <= 1) return "none";        // one weft: nothing to choose
   if (!figureOnLine) return "none";         // plain ground line: box stays white
   return (BOX_TABLE[weftCount] || [])[weftIndex] || "none";
@@ -1491,7 +1599,12 @@
 // Each weft may carry its own box pattern. Until the user changes one, it comes
 // from the standard table for however many wefts are loaded.
 function defaultBoxPattern(count, weftIndex, weftCount) {
-  return boxRow(count, weftIndex, weftCount, true);
+  return boxRow(count, weftIndex, weftCount, true, boxScheme());
+}
+
+// Which company's way of working the box this loom follows.
+function boxScheme() {
+  return (state.opts && state.opts.boxScheme) || "default";
 }
 
 // A pattern set by hand is kept, first for this session and then in the
@@ -1513,8 +1626,8 @@
   return defaultBoxPattern(count, weftIndex, weftCount);
 }
 
-function boxRow(count, weftIndex, weftCount, figureOnLine) {
-  const plan = boxPlan(weftCount, weftIndex, figureOnLine !== false);
+function boxRow(count, weftIndex, weftCount, figureOnLine, scheme) {
+  const plan = boxPlan(weftCount, weftIndex, figureOnLine !== false, scheme || boxScheme());
   const row = new Uint8Array(count);
   const half = Math.ceil(count / 2);
   if (plan === "all")    row.fill(1);
@@ -1584,6 +1697,27 @@
     if (o.boxWholeBand) closeBandGaps(figureOnLine);
   }
 
+  // Sri Tex ask a narrower question: not "is there a figure anywhere on this
+  // line" but "is there anything to weave on THIS weft". Each shuttle answers
+  // for itself, and a weft with nothing on it takes the one-design setting
+  // rather than dropping its box pins.
+  let figureByWeft = null;
+  if (target === "body" && boxScheme() === "sritex") {
+    figureByWeft = [];
+    for (let k = 0; k < K; k++) {
+      const flags = new Uint8Array(H);
+      const f = wefts[k] && wefts[k].file;
+      if (f) {
+        for (let y = 0; y < H && y < f.height; y++) {
+          const base = y * f.width;
+          for (let x = 0; x < f.width; x++) if (f.bits[base + x]) { flags[y] = 1; break; }
+        }
+      }
+      if (o.boxWholeBand) closeBandGaps(flags);
+      figureByWeft.push(flags);
+    }
+  }
+
   // Achu goes in the border file when there is one, otherwise in the body.
   // In the body it also waits on the rani, since that is the ground it binds.
   const raniLoaded = !!(state.wefts[0] && state.wefts[0].file && !state.wefts[0].file.error);
@@ -1642,21 +1776,27 @@
         const r = achuRow(seg.count, achuRowIndex, o.achuStartsBlack);
         for (let x = 0; x < seg.count; x++) bits[rowBase + at + x] = r[x];
 
-      } else if (seg.kind === "locking") {
-        if (target !== "body") return;              // locking lives in the body file only
+      } else if (usesWeave(seg.kind)) {
+        if (target !== "body") return;              // a woven group lives in the body file only
         const r = weaveRow(weaveById(seg.weave), seg.count, lockRowIndex, o.satinWarpFaced);
         for (let x = 0; x < seg.count; x++) bits[rowBase + at + x] = r[x];
 
       } else if (seg.kind === "box") {
         if (target !== "body") return;              // and so does the box
-        let fig = !!figureOnLine[designRow];
+        const scheme = boxScheme();
+        const flags = (scheme === "sritex" && figureByWeft) ? figureByWeft[weftIdx] : figureOnLine;
+        let fig = !!(flags && flags[designRow]);
         if (perShuttle > 1) {                       // hold across the shuttle's own run
           const start = Math.floor(designRow / perShuttle) * perShuttle;
           fig = false;
-          for (let d = start; d < Math.min(H, start + perShuttle); d++) if (figureOnLine[d]) { fig = true; break; }
+          for (let d = start; d < Math.min(H, start + perShuttle); d++) if (flags && flags[d]) { fig = true; break; }
         }
-        const active = K < 2 ? true : fig;
-        const pat = weftBoxPattern(wefts[weftIdx], seg.count, weftIdx, Math.max(1, K));
+        const active = scheme === "sritex" ? true : (K < 2 ? true : fig);
+        const pat = scheme === "sritex"
+          ? (storedBoxFor(wefts[weftIdx], seg.count)
+              ? weftBoxPattern(wefts[weftIdx], seg.count, weftIdx, Math.max(1, K))
+              : boxRow(seg.count, weftIdx, Math.max(1, K), fig, "sritex"))
+          : weftBoxPattern(wefts[weftIdx], seg.count, weftIdx, Math.max(1, K));
         const r = active ? pat : new Uint8Array(seg.count);
         for (let x = 0; x < seg.count; x++) bits[rowBase + at + x] = r[x];
 
@@ -1868,7 +2008,7 @@
     if (!s.count || s.count < 1) errs.push(`${where}: enter a pin count above zero.`);
     if (s.kind === "achu" && s.count % 2) errs.push(`${where}: achu needs an even count — ${s.count} cannot split in half.`);
     if (s.kind === "box" && s.count % 2) errs.push(`${where}: ${s.count} box pins cannot split into two sides. Enter an even number.`);
-    if (s.kind === "locking") {
+    if (usesWeave(s.kind)) {
       const w = weaveById(s.weave);
       if (!w) warns.push(`${where}: no weave chosen, so a satin is worked out from the pin count.`);
       else if (s.count % w.pins)
@@ -2156,6 +2296,29 @@
   return false;
 }
 
+const CREDITS_KEY = () => keyFor("credits");
+let memCredits = null;
+
+async function loadCredits() {
+  if (hasStore) {
+    const r = await readJSON(CREDITS_KEY());
+    state.credits = (r.found && !r.bad && r.value && typeof r.value === "object")
+      ? mergeCredits(freshCredits(), r.value)   // an old or short record is filled out
+      : freshCredits();
+    return;
+  }
+  state.credits = memCredits || freshCredits();
+}
+
+async function persistCredits() {
+  memCredits = state.credits;
+  if (hasStore) {
+    try { await store.set(CREDITS_KEY(), JSON.stringify(state.credits)); return true; }
+    catch { return false; }
+  }
+  return false;
+}
+
 const WEAVES_KEY = () => keyFor("weaves");
 
 async function loadWeaves() {
@@ -2239,6 +2402,92 @@
   notify("Downloaded. Keep that file — it holds whatever could not be read.");
 }
 
+// The balance, and a way to add to it. Kept in the header beside the account,
+// because it belongs to the account rather than to any one loom.
+function renderCredits() {
+  const host = el("#credits");
+  if (!host) return;
+  const left = creditsLeft();
+  host.className = "credits" + (left === 0 ? " out" : left <= 5 ? " low" : "");
+  host.innerHTML = left === 0
+    ? `No credits left <button class="tiny ghost" id="recharge">Recharge</button>`
+    : `<span><b>${left}</b> credit${left === 1 ? "" : "s"} left</span>
+       <button class="tiny ghost" id="recharge">Recharge</button>`;
+  const b = el("#recharge");
+  if (b) b.onclick = askForCode;
+
+  // a download that cannot be paid for should not look available
+  const short = left < COST_PER_FILE;
+  const db = el("#download"), bb = el("#downloadBorder");
+  if (db && state.built) db.disabled = short;
+  if (bb && state.builtBorder) bb.disabled = short;
+}
+
+// Spend, or say plainly why not. Every download goes through here.
+function payFor(what) {
+  if (spendCredits(COST_PER_FILE)) return true;
+  say(`No credits left, so the ${what} file cannot be downloaded.\n\n` +
+      `Building, previewing and checking against a file you trust all stay free — ` +
+      `only a finished file costs a credit. Press Recharge to add more.`, "Out of credits");
+  renderCredits();
+  return false;
+}
+
+/* ------------------------------------------------------------
+   Asking, in the app's own voice
+
+   say()     one button, for something that has gone wrong
+   ask()     two buttons, for something that cannot be undone
+   askText() a box to type in
+
+   All three hand back a promise, so a caller reads much as it did with
+   the browser's own alert and confirm — with an await in front.
+   ------------------------------------------------------------ */
+
+let askClose = null;
+
+function openAsk({ title, body, input, yes, no, danger }) {
+  const host = el("#ask");
+  if (!host) return Promise.resolve(no ? false : undefined);   // nothing to show it in
+  const was = document.activeElement;
+  el("#askTitle").textContent = title;
+  el("#askBody").textContent = body;
+  const box = el("#askInput");
+  box.hidden = !input; box.value = "";
+  if (input) box.placeholder = input.placeholder || "";
+  const yesBtn = el("#askYes"), noBtn = el("#askNo");
+  yesBtn.textContent = yes || "OK";
+  yesBtn.className = danger ? "solid danger" : "solid";
+  noBtn.hidden = !no; noBtn.textContent = no || "Cancel";
+  host.hidden = false;
+  (input ? box : yesBtn).focus();
+
+  return new Promise(resolve => {
+    const done = (v) => {
+      host.hidden = true;
+      document.removeEventListener("keydown", onKey, true);
+      askClose = null;
+      if (was && was.focus) was.focus();     // back where the person was
+      resolve(v);
+    };
+    askClose = () => done(input ? null : (no ? false : undefined));
+    yesBtn.onclick = () => done(input ? (box.value || "") : (no ? true : undefined));
+    noBtn.onclick = askClose;
+    host.onclick = (e) => { if (e.target === host) askClose(); };
+    function onKey(e) {
+      if (e.key === "Escape") { e.preventDefault(); askClose(); }
+      else if (e.key === "Enter" && (input || !no)) { e.preventDefault(); yesBtn.click(); }
+    }
+    document.addEventListener("keydown", onKey, true);
+  });
+}
+
+const say     = (body, title) => openAsk({ title: title || "Just so you know", body });
+const ask     = (body, o = {}) => openAsk({ title: o.title || "Are you sure?", body,
+                                            yes: o.yes || "Yes", no: o.no || "Cancel", danger: o.danger });
+const askText = (body, o = {}) => openAsk({ title: o.title || "", body, yes: o.yes || "Done",
+                                            no: "Cancel", input: { placeholder: o.placeholder || "" } });
+
 function setSaveState(kind) {
   const host = el("#saveState");
   if (!host) return;
@@ -2349,6 +2598,7 @@
       const committed = await autoCommitLoom();
       await persistSession();
       await persistBoxPrefs();
+      await persistCredits();
       if (committed) { await persistLooms(); scheduleSync(); }
       if (committed && state.screen === "looms") renderLoomList();
     } while (saveAgain);
@@ -2398,6 +2648,8 @@
   const list = segs || [];
   const at = list.findIndex(s => s && s.role === "clutch");
   if (at < 0) return "none";                 // no group, so no clutch was set
+  // Looms saved before the side was recorded: read it from where the group
+  // was put at the time. New looms always place it at the left regardless.
   return at >= list.length - 1 ? "right" : "left";
 }
 
@@ -2405,6 +2657,101 @@
   return ((b && b.savedAt) || "") > ((a && a.savedAt) || "") ? b : a;
 }
 
+/* ------------------------------------------------------------
+   Credits
+
+   A credit is spent when a finished file is downloaded — the border file
+   and the body file are one each. Building, previewing and checking
+   against a file you trust are all free, on purpose: those are the steps
+   that catch a wrong pin map, and charging for them would teach people
+   to skip the very thing that saves the silk.
+
+   Two numbers are kept, not one. Both only ever go up, so two machines
+   working the same account can be merged without either losing a spend
+   or inventing a credit: take the larger of each and subtract.
+
+   THIS IS A METER, NOT A LOCK. Everything here runs in the weaver's own
+   browser, so the numbers are theirs to edit and the page runs offline
+   with no network at all. It counts honest use and shows what is left.
+   Making it binding needs the conversion itself to happen somewhere the
+   weaver does not control, which is a different shape of product.
+   Everything below is arranged so that day only replaces spendCredits().
+   ------------------------------------------------------------ */
+
+const CREDITS_ON_JOINING = 25;      // what a new account starts with
+const COST_PER_FILE = 1;            // a finished BMP, border or body
+
+const freshCredits = () => ({ granted: CREDITS_ON_JOINING, spent: 0, codes: [] });
+const creditsLeft = () => {
+  const c = state.credits || freshCredits();
+  return Math.max(0, (c.granted || 0) - (c.spent || 0));
+};
+
+// Both halves only climb, so merging is taking whichever side has seen more.
+function mergeCredits(a, b) {
+  const A = a || freshCredits(), B = b || freshCredits();
+  return {
+    granted: Math.max(A.granted || 0, B.granted || 0),
+    spent:   Math.max(A.spent   || 0, B.spent   || 0),
+    // every code either side has seen, so one cannot be spent twice by
+    // recharging on two machines at once
+    codes: [...new Set([...(A.codes || []), ...(B.codes || [])])]
+  };
+}
+
+
+/* ------------------------------------------------------------
+   Recharge codes
+
+   A stand-in until payment is wired up, so accounts can be topped up by
+   hand and the whole path can be walked end to end. A code carries the
+   number of credits and a check made from that number and the secret
+   below, which stops a mistyped code from working — it does NOT stop
+   anyone reading this page from writing their own. It is not meant to.
+
+   When payment arrives, redeemCode is what it replaces: the amount comes
+   back from the payment provider instead of out of the code.
+   ------------------------------------------------------------ */
+
+const CODE_SALT = "pinmap-2026";     // change this and every code issued stops working
+
+// The check is made from the amount AND the serial, so each code issued is a
+// different string. One code for "50 credits" that everybody shares would be a
+// password for free credits, not a recharge.
+function codeCheck(amount, serial) {
+  let h = 0;
+  const t = `${amount}:${serial}:${CODE_SALT}`;
+  for (let i = 0; i < t.length; i++) h = (h * 33 + t.charCodeAt(i)) >>> 0;
+  return h.toString(36).toUpperCase().slice(0, 4).padStart(4, "0");
+}
+
+const newSerial = () =>
+  Math.floor(Math.random() * 36 ** 5).toString(36).toUpperCase().padStart(5, "0");
+
+// makeCode(50) gives a fresh one; makeCode(50, "AB12X") repeats a known one.
+const makeCode = (amount, serial) => {
+  const sn = (serial || newSerial()).toUpperCase();
+  return `PIN-${amount}-${sn}-${codeCheck(amount, sn)}`;
+};
+
+// a batch to print and hand out, each one different
+const makeCodes = (amount, howMany) =>
+  Array.from({ length: howMany }, () => makeCode(amount));
+
+// { ok, amount, code } or { ok:false, why }
+function readCode(raw) {
+  const t = String(raw || "").trim().toUpperCase().replace(/\s+/g, "");
+  const m = /^PIN-(\d{1,5})-([A-Z0-9]{4,6})-([A-Z0-9]{4})$/.exec(t);
+  if (!m) return { ok: false, why: "That is not a recharge code. They look like PIN-50-AB12X-9QR3." };
+  const amount = parseInt(m[1], 10);
+  if (!amount) return { ok: false, why: "That code is for no credits at all." };
+  if (m[3] !== codeCheck(amount, m[2])) return { ok: false, why: "That code did not check out. Try typing it again." };
+  if (usedCodes().includes(t)) return { ok: false, why: "That code has already been used on this account." };
+  return { ok: true, amount, code: t };
+}
+
+const usedCodes = () => (state.credits && state.credits.codes) || [];
+
 function mergeLibraries(local, remote) {
   local = local || {}; remote = remote || {};
   const lTime = local.updatedAt || "";
@@ -2442,6 +2789,8 @@
   return {
     looms: [...looms.values()],
     weaves: [...weaves.values()],
+    // credits are not settings — neither side may lose a spend or gain a grant
+    credits: mergeCredits(local.credits, remote.credits),
     boxPrefs,
     graveyard: [...graves].map(([name, deletedAt]) => ({ name, deletedAt })),
     updatedAt: rTime > lTime ? rTime : lTime
@@ -2505,6 +2854,28 @@
     cloud.auth = authMod.getAuth(cloud.app);
     cloud.db = dbMod.getFirestore(cloud.app);
     cloud.mod = { ...authMod, ...dbMod };
+
+    // Firebase keeps the sign-in in this browser, but nothing here ever asked
+    // it for one. Without this listener the app woke with no user every single
+    // time and sent the weaver back to Google, on a machine that had never
+    // signed out. Wait for the first answer before saying the account is off.
+    try {
+      if (authMod.browserLocalPersistence)
+        await authMod.setPersistence(cloud.auth, authMod.browserLocalPersistence);
+    } catch { /* the default is already local; carry on */ }
+
+    const settled = await withTimeout(new Promise(resolve => {
+      let first = true;
+      authMod.onAuthStateChanged(cloud.auth, user => {
+        cloud.user = user || null;
+        if (first) { first = false; resolve(true); return; }
+        // Later changes: a session that expired, or a sign-out somewhere else.
+        if (!cloud.user) setSyncState("off", "The sign-in has run out. Press Reconnect to sign in again.");
+        else scheduleSync();
+      });
+    }), CLOUD_WAIT_MS);
+    if (settled === TIMED_OUT) cloud.lastError = new Error("Firebase did not answer in time.");
+
     cloud.ready = true;
     return true;
   } catch (err) {
@@ -2554,10 +2925,40 @@
 
 /* ---------- what this device holds, in one shape ---------- */
 
+// The one place a credit is taken. A server would answer here instead.
+function spendCredits(n) {
+  const c = state.credits || (state.credits = freshCredits());
+  if (creditsLeft() < n) return false;
+  c.spent = (c.spent || 0) + n;
+  persistCredits();                 // straight away: a spend must not wait on a debounce
+  renderCredits(); scheduleSave(); scheduleSync();
+  return true;
+}
+
+async function askForCode() {
+  const raw = await askText("Enter your recharge code.", { title: "Recharge", placeholder: "PIN-50-AB12X-9QR3" });
+  if (raw === null) return;
+  const r = readCode(raw);
+  if (!r.ok) { await say(r.why, "That code did not work"); return; }
+  const c = state.credits || (state.credits = freshCredits());
+  c.codes = usedCodes().concat(r.code);      // so the same code cannot be used twice
+  grantCredits(r.amount);
+  notify(`${r.amount} credits added. ${creditsLeft()} left.`);
+}
+
+function grantCredits(n) {
+  const c = state.credits || (state.credits = freshCredits());
+  c.granted = (c.granted || 0) + n;
+  persistCredits();
+  renderCredits(); scheduleSave(); scheduleSync();
+}
+
+
 function localLibrary() {
   return {
     looms: state.looms,
     weaves: state.weaves.map(dryWeave),
+    credits: state.credits || freshCredits(),
     boxPrefs: state.boxPrefs,
     graveyard: state.graveyard || [],
     updatedAt: state.libraryUpdatedAt || ""
@@ -2568,9 +2969,11 @@
   state.looms = lib.looms || [];
   state.weaves = (lib.weaves || []).map(reviveWeave);
   state.boxPrefs = lib.boxPrefs || {};
+  state.credits = mergeCredits(state.credits, lib.credits);
   state.graveyard = lib.graveyard || [];
   state.libraryUpdatedAt = lib.updatedAt || new Date().toISOString();
   await persistLooms(); await persistWeaves(); await persistBoxPrefs(); await persistGraveyard();
+  await persistCredits(); renderCredits();
 }
 
 let syncTimer = null;
@@ -2863,6 +3266,16 @@
 
 // A returning visitor is already past the sign-in screen, so ask Google
 // quietly for a fresh token to talk to the shared store with.
+// On waking, look for the sign-in this browser already holds before troubling
+// Google for a new one. Only when there is genuinely none does the prompt go up.
+async function restoreCloud() {
+  setSyncState("busy");
+  const ready = await cloudStart();
+  if (ready && cloud.user) { await syncNow(); return true; }
+  await reconnectCloud();
+  return false;
+}
+
 async function reconnectCloud() {
   const clientId = CLIENT_ID_IS_BUILT_IN ? DEFAULT_CLIENT_ID : ((el("#clientId").value || "").trim());
   if (!clientId || (typeof navigator !== "undefined" && navigator.onLine === false)) {
@@ -2894,11 +3307,15 @@
     : hasStore
       ? "Sign out? Your looms stay on this device and come back when you sign in again."
       : "Sign out? Nothing is being kept on this device, so back up first if you want to keep this work.";
-  if (!confirm(warning)) return;
+  if (!await ask(warning, { title: "Sign out", yes: "Sign out", danger: true })) return;
   clearTimeout(saveTimer);
   await doAutoSave();
   try { if (window.google && window.google.accounts && window.google.accounts.id)
           window.google.accounts.id.disableAutoSelect(); } catch {}
+  // The session Firebase keeps has to go as well, or the restore on the next
+  // boot signs straight back into the account just left.
+  try { if (cloud.auth && cloud.mod.signOut) await withTimeout(cloud.mod.signOut(cloud.auth), CLOUD_WAIT_MS); } catch {}
+  cloud.user = null;
   state.account = null; state.accountKey = "local";
   if (hasStore) { try { await store.delete(ACCOUNT_KEY); } catch {} }
   memAccount = null;
@@ -3086,7 +3503,7 @@
   }
   if (act === "exp") { downloadJSON(doc); return; }
   if (act === "del") {
-    if (!confirm(`Delete the pin map for "${doc.name}"?`)) return;
+    if (!await ask(`Delete the pin map for "${doc.name}"?`, { title: "Delete loom", yes: "Delete", danger: true })) return;
     state.looms = state.looms.filter(l => l.name !== name);
     state.graveyard = (state.graveyard || []).filter(g => g.name !== name)
       .concat([{ name, deletedAt: new Date().toISOString() }]);
@@ -3111,8 +3528,12 @@
 const clutchAt = (segs) => (segs || []).findIndex(s => s && s.role === "clutch");
 const clutchSeg = () => state.segments[clutchAt(state.segments)] || null;
 
-// Put the empty group at the clutch end, or carry the existing one across.
-// A count that has been changed is kept — the end moves, the size is yours.
+// Put the empty group in, or carry the existing one across.
+//
+// It always goes at the left of the board, whichever side the clutch is on.
+// The clutch end is a fact about the loom and worth recording, but it does not
+// decide where these pins sit: on a Sri Tex loom they are the first thirty-two
+// either way. A count that has been changed is kept — the size is yours.
 function applyClutchGroup(side) {
   const segs = state.segments;
   const at = clutchAt(segs);
@@ -3122,7 +3543,7 @@
   seg.role = "clutch";
   seg.kind = "empty";
   if (!seg.count || seg.count < 1) seg.count = CLUTCH_PINS;
-  if (normalClutch(side) === "right") segs.push(seg); else segs.unshift(seg);
+  segs.unshift(seg);
   return seg;
 }
 
@@ -3178,7 +3599,7 @@
   const f = e.target.files && e.target.files[0]; if (!f) return;
   let doc;
   try { doc = JSON.parse(await f.text()); }
-  catch (err) { alert("Could not read that file: " + err.message); e.target.value = ""; return; }
+  catch (err) { say("Could not read that file: " + err.message, "That file would not open"); e.target.value = ""; return; }
 
   if (doc && doc.format === "pinmap.backup") {
     const incoming = Array.isArray(doc.looms) ? doc.looms : [];
@@ -3205,7 +3626,7 @@
     adoptDoc(doc);
     state.editing = state.looms.some(l => l.name === state.loomName) ? state.loomName : null;
     go("loom");
-  } catch (err) { alert("Could not read that file: " + err.message); }
+  } catch (err) { say("Could not read that file: " + err.message, "That file would not open"); }
   e.target.value = "";
 };
 
@@ -3291,7 +3712,7 @@
   const pitch = zoom === "fit"
     ? Math.max(4, Math.min(24, (Math.max(260, host.clientWidth - 28) - 40) / cols))
     : parseFloat(zoom);
-  const RULE = 22, GUT = 40, FOOT = 18;
+  const RULE = 22, GUT = 40, FOOT = 34;
   const W = GUT + cols * pitch + 8, H = RULE + PINS_PER_COLUMN * pitch + FOOT;
 
   host.innerHTML = `<div class="board-wrap"><canvas id="${canvasId}"${interactive ? ' tabindex="0"' : ""}
@@ -3449,14 +3870,6 @@
     }
   });
 
-  /* ---- which way the map runs, said outright ---- */
-  ctx.font = "10.5px ui-monospace, Menlo, Consolas, monospace";
-  ctx.fillStyle = "#4A555E";
-  ctx.textAlign = "left"; ctx.textBaseline = "alphabetic";
-  ctx.fillText("pin 1", GUT, RULE - 7);
-  ctx.textAlign = "right";
-  ctx.fillText("pin " + T.toLocaleString() + " \u2192", GUT + cols * pitch, RULE - 7);
-
   /* ---- a scale along the foot, so the shape of the loom is readable ---- */
   const fy = RULE + PINS_PER_COLUMN * pitch + 7;
   let at = 0;
@@ -3474,6 +3887,41 @@
     ctx.strokeRect(GUT + (at / T) * (cols * pitch) + 0.5, fy + 0.5, Math.max(1, ((T - at) / T) * (cols * pitch) - 1), 5);
   }
 
+  /* ---- which end is which, named under the board ----
+
+     The board reads down a column of sixteen and then on to the next, so which
+     end of the loom you are looking at is not obvious. It used to be written on
+     the ruler line, where the column numbers were drawn straight over it. It
+     goes underneath now, with the edge of the loom named, because the pin map
+     runs the way the pins are numbered and that is not always left to right on
+     the machine. */
+  const barL = GUT, barR = GUT + cols * pitch;
+  ctx.strokeStyle = "#9AA6AD"; ctx.lineWidth = 1;
+  ctx.beginPath();
+  ctx.moveTo(barL + 0.5, fy - 2); ctx.lineTo(barL + 0.5, fy + 8);
+  ctx.moveTo(barR - 0.5, fy - 2); ctx.lineTo(barR - 0.5, fy + 8);
+  ctx.stroke();
+
+  const nearEdge = state.opts && state.opts.pinOneLeft === false ? "right" : "left";
+  const farEdge  = nearEdge === "left" ? "right" : "left";
+  const ty = fy + 20;
+  ctx.font = "10.5px ui-monospace, Menlo, Consolas, monospace";
+  ctx.textBaseline = "alphabetic";
+  ctx.fillStyle = "#4A555E";
+
+  // Measure rather than guess at a width: the two ends must not meet in the
+  // middle, and how much room a word takes is not something to estimate.
+  const pick = (long, short) => [long, short];
+  const [lLong, lShort] = pick(`\u25c0 pin 1 \u00b7 ${nearEdge} edge of the loom`, "\u25c0 pin 1");
+  const [rLong, rShort] = pick(`pin ${T.toLocaleString()} \u00b7 ${farEdge} edge \u25b6`,
+                               `pin ${T.toLocaleString()} \u25b6`);
+  const fits = (a, b) => ctx.measureText(a).width + ctx.measureText(b).width + 24 <= (barR - barL);
+  const [lTxt, rTxt] = fits(lLong, rLong) ? [lLong, rLong]
+                     : fits(lShort, rShort) ? [lShort, rShort]
+                     : ["\u25c0 1", String(T.toLocaleString()) + " \u25b6"];
+  ctx.textAlign = "left";  ctx.fillText(lTxt, barL, ty);
+  ctx.textAlign = "right"; ctx.fillText(rTxt, barR, ty);
+
   return { pitch, T, owners, W, H, spans, bounds, cols, GUT, RULE, cx, cy, xy };
 }
 
@@ -3562,7 +4010,7 @@
   const sg = state.segments[idx];
   if (!sg) return n;
   if (sg.kind === "achu") return Math.max(2, Math.round(n / 2) * 2);
-  if (sg.kind === "locking") {
+  if (usesWeave(sg.kind)) {
     const wv = weaveById(sg.weave);
     if (wv && wv.pins) return Math.max(wv.pins, Math.round(n / wv.pins) * wv.pins);
   }
@@ -3689,7 +4137,7 @@
   const sg = state.segments[idx];
   if (!sg) return 1;
   if (sg.kind === "achu") return 2;
-  if (sg.kind === "locking") { const wv = weaveById(sg.weave); return wv && wv.pins ? wv.pins : 1; }
+  if (usesWeave(sg.kind)) { const wv = weaveById(sg.weave); return wv && wv.pins ? wv.pins : 1; }
   return 1;
 }
 
@@ -3779,7 +4227,13 @@
     host.innerHTML = `<div class="wl-empty">Nothing uploaded. The built-in satins and twills are always in the Locking row.</div>`;
     return;
   }
-  host.innerHTML = `<div class="wl-grid">` + state.weaves.map(w => `
+  // An uploaded weave is only ever chosen on a Locking group. Say so when there
+  // is nowhere to put one, or the weave looks as though it went nowhere.
+  const noHome = !state.segments.some(sg => sg.kind === "weaveLib");
+  const where = noHome
+    ? `<div class="wl-empty">Nowhere to use these yet. Add a <strong>Weave library</strong> group to the pin map and these appear in its list.</div>`
+    : "";
+  host.innerHTML = where + `<div class="wl-grid">` + state.weaves.map(w => `
     <div class="wl-item">
       <canvas data-weave="${esc(w.id)}" role="img" aria-label="The ${esc(w.name)} weave, ${w.pins} pins"></canvas>
       <div style="flex:1;min-width:0">
@@ -3794,24 +4248,25 @@
 el("#addWeave").onclick = async () => {
   const name = el("#weaveName").value.trim();
   const f = el("#weaveFile").files && el("#weaveFile").files[0];
-  if (!name) { alert("Give the weave a name first."); return; }
-  if (!f) { alert("Choose a BMP of the weave."); return; }
+  if (!name) { await say("Give the weave a name first.", "Nearly"); return; }
+  if (!f) { await say("Choose a BMP of the weave.", "Nearly"); return; }
   let img;
   try {
     const buf = await f.arrayBuffer();
     try { img = decodeBMP(buf); }
     catch (err) { err.fromDecoder = true; throw err; }
   } catch (err) {
-    alert(err.fromDecoder ? "Could not read that file: " + err.message
-                          : "Could not read that file — check it is an uncompressed BMP.");
+    await say(err.fromDecoder ? "Could not read that file: " + err.message
+                             : "Could not read that file — check it is an uncompressed BMP.",
+              "That file would not open");
     return;
   }
   if (img.width > 128 || img.height > 128) {
-    alert(`That is ${img.width} × ${img.height}. A weave should be one repeat, no more than 128 either way.`);
+    await say(`That is ${img.width} × ${img.height}. A weave should be one repeat, no more than 128 either way.`, "Too big for a weave");
     return;
   }
   if (state.weaves.some(w => w.name.toLowerCase() === name.toLowerCase()) &&
-      !confirm(`A weave called "${name}" already exists. Add another with the same name?`)) return;
+      !await ask(`A weave called "${name}" already exists. Add another with the same name?`, { title: "Same name" })) return;
 
   state.weaves.push({ id: "w" + Date.now().toString(36) + Math.floor(Math.random() * 1e4).toString(36),
                       name, pins: img.width, kind: "custom",
@@ -3826,7 +4281,7 @@
 el("#weaveList").addEventListener("click", async e => {
   const t = e.target.closest("button[data-act=delweave]"); if (!t) return;
   const w = weaveById(t.dataset.id); if (!w) return;
-  if (!confirm(`Remove "${w.name}" from the library?`)) return;
+  if (!await ask(`Remove "${w.name}" from the library?`, { title: "Remove weave", yes: "Remove", danger: true })) return;
   state.weaves = state.weaves.filter(x => x.id !== t.dataset.id);
   state.segments.forEach(sg => { if (sg.weave === t.dataset.id) delete sg.weave; });
   await persistWeaves();
@@ -3857,18 +4312,24 @@
   state.segments.forEach((sg, i) => {
     const from = at, to = at + (sg.count || 0) - 1; at += (sg.count || 0);
     let content;
-    if (sg.kind === "locking") {
+    if (usesWeave(sg.kind)) {
+      // Each of the two groups offers its own weaves and nothing else. Locking
+      // works a built-in satin or twill; Weave library works one that has been
+      // uploaded. Mixing them in a single list is how an uploaded weave came to
+      // be invisible to anyone who had not been shown where it hides.
+      const lib   = sg.kind === "weaveLib";
+      const list  = lib ? state.weaves : BUILTIN_WEAVES;
+      const empty = lib && !state.weaves.length;
       const chosen = weaveById(sg.weave);
+      const mine = !!(chosen && list.some(w => w.id === chosen.id));
       content = `<div class="weave-cell">
         <canvas data-rowweave="${sg.id}" role="img" aria-label="The weave chosen for this group"></canvas>
-        <select data-act="weave" data-id="${sg.id}" style="flex:1">
-          <optgroup label="Satins and twills">
-            ${BUILTIN_WEAVES.map(w => `<option value="${esc(w.id)}" ${w.id===sg.weave?"selected":""}>${esc(w.name)} · ${w.pins} pins</option>`).join("")}
-          </optgroup>
-          ${state.weaves.length ? `<optgroup label="Your weaves">
-            ${state.weaves.map(w => `<option value="${esc(w.id)}" ${w.id===sg.weave?"selected":""}>${esc(w.name)} · ${w.pins} pins</option>`).join("")}
-          </optgroup>` : ""}
-          ${chosen ? "" : `<option value="" selected>— choose a weave —</option>`}
+        <select data-act="weave" data-id="${sg.id}" style="flex:1" ${empty ? "disabled" : ""}
+                aria-label="Weave for ${esc(labelFor(state.segments, i))}">
+          <option value="" ${mine ? "" : "selected"}>${empty
+            ? "Nothing uploaded yet — add one above"
+            : (lib ? "Choose from the library…" : "Choose a satin or twill…")}</option>
+          ${list.map(w => `<option value="${esc(w.id)}" ${mine && w.id === sg.weave ? "selected" : ""}>${esc(w.name)} · ${w.pins} pins</option>`).join("")}
         </select></div>`;
     } else {
       const note = (sg.role === "clutch" && companyHasClutch(state.company))
@@ -3913,8 +4374,8 @@
 }
 els("[data-add]").forEach(b => b.onclick = () => addSeg(b.dataset.add));
 
-el("#starter").onclick = () => {
-  if (state.segments.length && !confirm("Replace the current groups with the standard order?")) return;
+el("#starter").onclick = async () => {
+  if (state.segments.length && !await ask("Replace the current groups with the standard order?", { title: "Start again", yes: "Replace" })) return;
   applyDefaultLayout();
   if (companyHasClutch(state.company) && state.clutch !== "none") applyClutchGroup(state.clutch);
   state.built = null; state.builtBorder = null; renderLoomScreen();
@@ -3922,15 +4383,15 @@
 
 el("#restToBody").onclick = () => {
   const left = (state.totalDeclared || 0) - allocated();
-  if (left <= 0) { alert("There are no spare pins to allocate."); return; }
+  if (left <= 0) { say("There are no spare pins to allocate.", "Nothing spare"); return; }
   const existing = state.segments.find(s => s.kind === "body");
   if (existing) existing.count = (existing.count || 0) + left;
   else state.segments.push({ id: nextId(), kind: "body", count: left });
   state.built = null; state.builtBorder = null; renderLoomScreen();
 };
 
-el("#clearSegs").onclick = () => {
-  if (state.segments.length && !confirm("Remove every group?")) return;
+el("#clearSegs").onclick = async () => {
+  if (state.segments.length && !await ask("Remove every group?", { title: "Clear the pin map", yes: "Remove all", danger: true })) return;
   state.segments = [];
   state.borderFiles = {};
   state.wefts = freshWefts();
@@ -3956,7 +4417,7 @@
   const seg = clutchSeg();
   if (note) note.textContent =
       side === "none" ? "No pins set aside. Say which end only if this loom really gives pins up to a clutch."
-    : seg ? `${seg.count} pins kept empty at the ${side} end — move or resize the group like any other.`
+    : seg ? `${seg.count} pins kept empty at the left of the board, whichever end the clutch is on — move or resize the group like any other.`
     : "The group has been removed. Pick an end again to put it back.";
 }
 
@@ -3966,7 +4427,7 @@
   let clutchMsg = "";
   if (companyHasClutch(state.company) && normalClutch(state.clutch) !== "none") {
     const seg = applyClutchGroup(state.clutch);
-    clutchMsg = ` ${seg.count} pins held at the ${normalClutch(state.clutch)} end.`;
+    clutchMsg = ` ${seg.count} pins held at the left of the board.`;
   } else if (removeClutchGroup(true)) {
     clutchMsg = " Clutch group removed.";
   }
@@ -3986,7 +4447,7 @@
     msg = removeClutchGroup(false) ? "Clutch group removed." : "No clutch pins.";
   } else {
     const seg = applyClutchGroup(state.clutch);
-    msg = `${clutchLabel(state.clutch)} — ${seg.count} pins at the ${state.clutch} end.`;
+    msg = `${clutchLabel(state.clutch)} — ${seg.count} pins at the left of the board.`;
   }
   state.built = null; state.builtBorder = null;
   renderLoomScreen(); scheduleSave();
@@ -4047,7 +4508,12 @@
     s.kind = t.value;
     if (s.kind !== "empty") delete s.role;      // it has been made into something else
     if (s.kind === "locking" && !s.weave) { s.weave = "satin:8:3"; s.count = 8; }
-    if (s.kind !== "locking") delete s.weave;
+    if (s.kind === "weaveLib") {
+      // only a library weave belongs here, so a built-in carried over is dropped
+      const w = weaveById(s.weave);
+      if (!w || !state.weaves.some(x => x.id === w.id)) delete s.weave;
+    }
+    if (!usesWeave(s.kind)) delete s.weave;
     forgetSideHighlights();
     if (s.kind === "achu" && !s.count) s.count = 4;
     if (s.kind === "box" && !s.count) s.count = 4;
@@ -4156,7 +4622,7 @@
     const idx = state.segments.indexOf(sg);
     let jump = 1;
     if (sg.kind === "achu") jump = 2;
-    else if (sg.kind === "locking") {
+    else if (usesWeave(sg.kind)) {
       const wv = weaveById(sg.weave);
       jump = wv && wv.pins ? wv.pins : 1;
     }
@@ -4198,7 +4664,7 @@
   clearTimeout(saveTimer);
   const name = state.loomName.trim();
   const clash = state.looms.find(l => l.name === name && l.name !== state.editing);
-  if (clash && !confirm(`A loom called "${name}" already exists. Replace its pin map?`)) return;
+  if (clash && !await ask(`A loom called "${name}" already exists. Replace its pin map?`, { title: "Same name", yes: "Replace" })) return;
 
   const doc = pinMapDoc();
   const prevIdx = state.looms.findIndex(l => l.name === (state.editing || name));
@@ -4523,7 +4989,7 @@
   const wefts = filledWefts();
   let html = "";
 
-  const lockSeg = state.segments.find(s => s.kind === "locking" && s.count > 0);
+  const lockSeg = state.segments.find(s => usesWeave(s.kind) && s.count > 0);
   if (lockSeg) {
     const lw = weaveById(lockSeg.weave);
     const n = lockSeg.count;
@@ -4546,9 +5012,9 @@
   host.innerHTML = html;
 }
 
-el("#clearFiles").onclick = () => {
+el("#clearFiles").onclick = async () => {
   if (!state.wefts.some(w => w.file)) return;
-  if (!confirm("Remove the body weft files?")) return;
+  if (!await ask("Remove the body weft files?", { title: "Clear files", yes: "Remove", danger: true })) return;
   state.wefts = freshWefts();
   state.built = null;
   renderBodyScreen();
@@ -4873,8 +5339,9 @@
     state.builtBorder = compose("border");
     renderBorderPreview();
     el("#downloadBorder").disabled = false;
+    renderCredits();   // and only while there is a credit to spend
     notify("Border file built.");
-  } catch (err) { alert("Could not combine the border: " + err.message); }
+  } catch (err) { say("Could not combine the border: " + err.message, "Could not combine"); }
 };
 
 el("#combineBody").onclick = () => {
@@ -4883,12 +5350,12 @@
     state.built = compose("body");
     renderPreview();
     notify("Body file built.");
-  } catch (err) { alert("Could not combine the body: " + err.message); }
+  } catch (err) { say("Could not combine the body: " + err.message, "Could not combine"); }
 };
 
-el("#clearBorder").onclick = () => {
+el("#clearBorder").onclick = async () => {
   if (!Object.keys(state.borderFiles).length) return;
-  if (!confirm("Remove the border files?")) return;
+  if (!await ask("Remove the border files?", { title: "Clear files", yes: "Remove", danger: true })) return;
   state.borderFiles = {};
   state.builtBorder = null;
   renderBorderScreen();
@@ -4922,8 +5389,9 @@
       const p = (y * w + x) * 4;
       const up = b.bits[sy * b.width + sx];
       let r, g, bl;
-      if (up) [r, g, bl] = REGION_COLOUR[b.region[sx]] || [20, 24, 28];
-      else r = g = bl = 255;
+      if (!up) r = g = bl = 255;
+      else if (state.pvColour) [r, g, bl] = REGION_COLOUR[b.region[sx]] || [20, 24, 28];
+      else r = g = bl = 0;                    // the file as the loom reads it
       img.data[p] = r; img.data[p+1] = g; img.data[p+2] = bl; img.data[p+3] = 255;
     }
   }
@@ -4931,11 +5399,28 @@
   if (borderZoom === "fit") { cv.style.width = "100%"; cv.style.height = "auto"; }
   el("#borderDims").textContent =
     `${b.width} pins × ${b.height} lines${step > 1 ? ` · preview reduced ${step}×` : ""}`;
+
+  el("#borderPvMode").setAttribute("aria-pressed", String(state.pvColour));
+  el("#borderPvMode").textContent = state.pvColour ? "True black & white" : "Colour groups";
+
+  const used = new Set(state.segments.map(sg => sg.kind));
+  el("#borderPvLegend").innerHTML = !state.pvColour ? "" :
+    [...used].map(k => `<span><i style="background:${KINDS[k].colour};border-radius:0"></i>${KINDS[k].label}</span>`).join("");
+
+  // The colours are here to read the file by. They are never written: the BMP
+  // is one bit a pin either way, which is all the loom will take.
+  el("#borderPvNote").textContent = state.pvColour
+    ? "Colours mark the groups. The downloaded BMP is black and white only."
+    : "This is the file as it downloads — one bit a pin, black and white.";
   el("#downloadBorder").disabled = false;
+  renderCredits();   // and only while there is a credit to spend
 }
 
+el("#borderPvMode").onclick = () => { state.pvColour = !state.pvColour; renderBorderPreview(); };
+
 el("#downloadBorder").onclick = () => {
   const b = state.builtBorder; if (!b) return;
+  if (!payFor("border")) return;
   const bytes = encodeBMP1(b.bits, b.width, b.height, state.opts.blackIsIndexZero);
   const safe = (state.loomName || "loom").replace(/[^A-Za-z0-9_-]+/g, "_").replace(/^_|_$/g, "");
   const url = URL.createObjectURL(new Blob([bytes], { type: "image/bmp" }));
@@ -5002,7 +5487,8 @@
   5: [122,78,134],  // locking
   6: [82,115,126],  // body
   7: [185,195,200], // empty
-  8: [235,240,242]  // not allocated
+  8: [235,240,242], // not allocated
+  9: [154,94,82]    // weave library
 };
 let pvStep = 1;
 
@@ -5013,7 +5499,7 @@
   const H = ref ? ref.height : 0;
   const perShuttle = (state.opts.stackMode === "interleave" && normalBoxMotion(state.boxMotion) === "4x1") ? 2 : 1;
   const lines = !H ? 0 : (state.opts.stackMode === "overlay" ? H : H * Math.max(1, K));
-  const lockSeg = state.segments.find(x => x.kind === "locking");
+  const lockSeg = state.segments.find(x => usesWeave(x.kind));
   const lw = lockSeg ? weaveById(lockSeg.weave) : null;
   const cell = (k, v, sm, full) =>
     `<div class="cell"><div class="k">${k}</div>
@@ -5079,6 +5565,7 @@
   el("#pvLegend").innerHTML = !state.pvColour ? "" :
     [...used].map(k => `<span><i style="background:${KINDS[k].colour};border-radius:0"></i>${KINDS[k].label}</span>`).join("");
   el("#download").disabled = false;
+  renderCredits();   // and only while there is a credit to spend
 }
 
 function applyZoom() {
@@ -5115,6 +5602,7 @@
 
 el("#download").onclick = () => {
   const b = state.built; if (!b) return;
+  if (!payFor("body")) return;
   const bytes = encodeBMP1(b.bits, b.width, b.height, state.opts.blackIsIndexZero);
   const safe = (state.loomName || "loom").replace(/[^A-Za-z0-9_-]+/g, "_").replace(/^_|_$/g, "");
   const url = URL.createObjectURL(new Blob([bytes], { type: "image/bmp" }));
@@ -5216,12 +5704,14 @@
   await loadLooms();
   await loadWeaves();
   await loadBoxPrefs();
+  await loadCredits();
   await loadGraveyard();
   await loadSession();
   setSaveState(hasStore ? "saved" : "off");
   setSyncState(CLOUD_CONFIGURED && state.account ? "off" : "ok");
   renderWhoami();
-  if (CLOUD_CONFIGURED && state.account && !cloud.user) reconnectCloud();
+  renderCredits();
+  if (CLOUD_CONFIGURED && state.account && !cloud.user) restoreCloud();
   renderOpts();
   renderStoreNote();
   // If anything would not read, that is the first thing to say, and it is said
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/extract.mjs ../pinmap/tests/extract.mjs
--- ../verify/tests/extract.mjs	2026-08-03 11:34:51.545163124 +0000
+++ ../pinmap/tests/extract.mjs	2026-08-04 05:59:44.598186728 +0000
@@ -61,6 +61,9 @@
   mirrorBits, flipBits, invertBits, state, KINDS, DEFAULT_LAYOUT, labelFor,
   mergeLibraries, newerOf, normalBoxMotion, normalClutch, clutchSideOf,
   COMPANIES, COMPANY_BASE, companyById, companyHasClutch, CLUTCH_PINS,
+  SRITEX_BOX,
+  freshCredits, creditsLeft, mergeCredits,
+  makeCode, makeCodes, readCode, codeCheck, newSerial, CREDITS_ON_JOINING, COST_PER_FILE,
   freshWefts, filledWefts, borderSlots, bodySlots,
   WEFT_SLOTS, WEFT_NAMES, totalPins, allocated
 };
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/logic/01.mjs ../pinmap/tests/logic/01.mjs
--- ../verify/tests/logic/01.mjs	2026-08-03 11:34:51.573163124 +0000
+++ ../pinmap/tests/logic/01.mjs	2026-08-03 18:25:28.444569241 +0000
@@ -87,7 +87,7 @@
 
 /* ------------------------------------------------------- groups and names */
 head('groups');
-ok('seven groups', Object.keys(KINDS).length === 7, String(Object.keys(KINDS).length));
+ok('eight groups', Object.keys(KINDS).length === 8, String(Object.keys(KINDS).length));
 ok('only the borders take a single file',
    Object.entries(KINDS).filter(([, v]) => v.source === 'border').map(([k]) => k).join(',') === 'leftBorder,rightBorder');
 ok('only body takes weft files',
@@ -96,9 +96,13 @@
    KINDS.achu.source === 'generated' && KINDS.box.source === 'generated');
 ok('locking is generated, empty never lifts',
    KINDS.locking.source === 'generated' && KINDS.empty.source === 'down');
+// Locking works a built-in satin; Weave library works an uploaded one. Same
+// behaviour, different list to pick from.
+ok('the weave library group is worked the same way as locking',
+   KINDS.weaveLib.source === 'generated');
 ok('codes and colours are unique',
-   new Set(Object.values(KINDS).map(k => k.code)).size === 7 &&
-   new Set(Object.values(KINDS).map(k => k.colour)).size === 7);
+   new Set(Object.values(KINDS).map(k => k.code)).size === 8 &&
+   new Set(Object.values(KINDS).map(k => k.colour)).size === 8);
 ok('standard order preloaded',
    DEFAULT_LAYOUT.map(g => g[0]).join(',') === 'achu,box,leftBorder,locking,body,rightBorder,achu,empty',
    DEFAULT_LAYOUT.map(g => g[0]).join(','));
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/logic/24.mjs ../pinmap/tests/logic/24.mjs
--- ../verify/tests/logic/24.mjs	2026-08-03 11:34:51.577163124 +0000
+++ ../pinmap/tests/logic/24.mjs	2026-08-03 16:23:47.196630256 +0000
@@ -58,7 +58,10 @@
   ok('the 752 pins of locking + body + locking land at 424-1175, as their file has them',
      run(u, 424, 1175), 'first worked pin of the block: ' + (u.indexOf(1) + 1));
   ok('nothing is worked at 35-423, where their left border goes', blank(u, 35, 423));
-  ok('nor at 1176-1552, where their right border goes', blank(u, 1176, 1552));
+  // The left border occupies 1176-1550 once mirrored; 1551-1558 is the box,
+  // which under the Sri Tex scheme lifts on every line, so it is not blank
+  // and never was meant to be part of this check.
+  ok('nor at 1176-1550, where the other border goes', blank(u, 1176, 1550));
 }
 
 head('written the same way round, it lands thirty pins short');
Only in ../pinmap/tests/logic: 25.mjs
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/ui/09-pinboard.mjs ../pinmap/tests/ui/09-pinboard.mjs
--- ../verify/tests/ui/09-pinboard.mjs	2026-08-03 11:34:51.581800249 +0000
+++ ../pinmap/tests/ui/09-pinboard.mjs	2026-08-03 17:38:09.115946544 +0000
@@ -26,7 +26,7 @@
 START.forEach((v,i)=>{ const el=$$('#segTable input[type=number]')[i]; if(el) setVal(el,String(v)); });
 await wait(600);
 
-const PER = 16, GUT = 40, RULE = 22, FOOT = 18, PAD = 8;
+const PER = 16, GUT = 40, RULE = 22, FOOT = 34, PAD = 8;   // the foot now names both ends
 const counts = () => $$('#segTable input[type=number]').map(e=>parseInt(e.value,10)||0);
 const cv = () => $('#board');
 const place = () => { const c=cv(); c.getBoundingClientRect = () => ({ left:0, top:0, width:c.width, height:c.height }); };
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/ui/14-audit.mjs ../pinmap/tests/ui/14-audit.mjs
--- ../verify/tests/ui/14-audit.mjs	2026-08-03 11:34:51.581800249 +0000
+++ ../pinmap/tests/ui/14-audit.mjs	2026-08-04 06:18:51.421518431 +0000
@@ -220,6 +220,52 @@
   note('including the pin-count steppers', !/\.stepper/.test(coarse));
 }
 
+console.log('\n== buttons ==');
+{
+  const css = w.document.querySelector('style').textContent;
+  note('a press is felt, not just hoped for', !/button:active:not\(:disabled\)/.test(css));
+  note('and hover does not snap', !/button\s*\{[^}]*transition:/.test(css));
+  // A delete button that looks like every other button is found by accident.
+  note('a destructive button says so before it is hovered',
+       !/button\.danger\s*\{[^}]*color:\s*var\(--meena\)/.test(css));
+  note('the grey tap flash is replaced, not merely removed',
+       !/tap-highlight-color/.test(css) || !/button:active/.test(css));
+  note('a double click does not select the label', !/user-select:\s*none/.test(css));
+}
+
+console.log('\n== type ==');
+{
+  const css = w.document.querySelector('style').textContent;
+  note('the screen title has a size of its own in the scale', !/--t-head/.test(css));
+  // iOS Safari zooms the whole page when a field under 16px takes focus, and
+  // the weaver has to pinch back out to see the pin map.
+  const coarse = (css.match(/@media \(pointer: coarse\)\s*\{[\s\S]*?\n  \}/) || [''])[0];
+  note('fields do not make a phone zoom in on focus',
+       !/font-size:\s*16px/.test(coarse) || !/input\[type=text\]/.test(coarse));
+  note('long titles are allowed to balance', !/text-wrap:\s*balance/.test(css));
+}
+
+console.log('\n== the app speaks for itself ==');
+{
+  const src = fs.readFileSync(process.argv[2],'utf8');
+  const js = src.split('<script>')[1] || '';
+  // A browser's own alert prints "x.test says" above the words and cannot be
+  // styled at all — it is the first thing that makes a page look like a page
+  // rather than a tool.
+  const natives = (js.match(/(?:^|[^.\w])(alert|confirm|prompt)\s*\(/g) || [])
+    .filter(m => !/accounts/.test(m));
+  note('nothing falls back to the browser\'s own dialogs', natives.length > 0, natives.join(' '));
+  note('there is a dialog of our own', !w.document.getElementById('ask'));
+  note('it is announced as one', (w.document.getElementById('ask')||{}).getAttribute
+       ? w.document.getElementById('ask').getAttribute('role') !== 'dialog' : true);
+  note('escape and enter are handled', !/e\.key === "Escape"/.test(js) || !/e\.key === "Enter"/.test(js));
+  note('focus goes back where it was', !/was && was\.focus/.test(js));
+
+  const css = w.document.querySelector('style').textContent;
+  note('the file buttons are ours, not the operating system\'s',
+       !/::file-selector-button/.test(css));
+}
+
 console.log('\n== result ==');
 if (found.length){ console.log('  '+found.length+' problem(s)'); found.forEach(e=>console.log('   - '+e)); }
 else console.log('  no errors');
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/ui/15-verify.mjs ../pinmap/tests/ui/15-verify.mjs
--- ../verify/tests/ui/15-verify.mjs	2026-08-03 11:34:51.581800249 +0000
+++ ../pinmap/tests/ui/15-verify.mjs	2026-08-04 06:18:06.329591612 +0000
@@ -31,7 +31,14 @@
 click($('#newLoom'));
 setVal($('#loomName'),'Verify'); setVal($('#totalDeclared'),'720');
 // one group, the whole loom is body, so it matches the sample exactly
-click($('#clearSegs')); await wait(200);
+// The app asks in its own dialog now rather than the browser's, so a question
+// has to be answered rather than stubbed away.
+const answerYes = async () => {
+  await wait(60);
+  const y = $('#askYes');
+  if (y && !$('#ask').hidden) { click(y); await wait(150); }
+};
+click($('#clearSegs')); await answerYes(); await wait(200);
 click($('[data-add=body]')); await wait(200);
 setVal($$('#segTable input[type=number]')[0], '720');
 await wait(600);
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/ui/17-clutch.mjs ../pinmap/tests/ui/17-clutch.mjs
--- ../verify/tests/ui/17-clutch.mjs	2026-08-03 11:34:51.581800249 +0000
+++ ../pinmap/tests/ui/17-clutch.mjs	2026-08-03 17:19:42.325923370 +0000
@@ -52,40 +52,41 @@
 console.log('\n== saying which end ==');
 setSel($('#loomClutch'), 'left'); await wait(350);
 ok('an empty group arrives', /the clutch/.test($('#segTable').textContent));
-ok('at the front, for a left clutch', kinds()[0] === 'empty', kinds().join(','));
+ok('at the left of the board', kinds()[0] === 'empty', kinds().join(','));
 ok('holding 32 pins', counts()[0] === '32', counts()[0]);
 ok('the note says where they are', /32 pins/.test($('#clutchNote').textContent) && /left/.test($('#clutchNote').textContent),
    $('#clutchNote').textContent);
 
-console.log('\n== the other end ==');
+console.log('\n== the clutch end does not move them ==');
+// The end the clutch sits on is worth recording, but on a Sri Tex loom the
+// pins are the first thirty-two either way.
 setSel($('#loomClutch'), 'right'); await wait(350);
-ok('the group moves to the back', kinds().slice(-1)[0] === 'empty' && counts().slice(-1)[0] === '32',
-   kinds().join(',')+' / '+counts().slice(-1)[0]);
-ok('and is no longer at the front', counts()[0] !== '32' || kinds()[0] !== 'empty', counts()[0]);
+ok('a right clutch keeps them at the left', kinds()[0] === 'empty' && counts()[0] === '32',
+   kinds().join(',')+' / '+counts()[0]);
+ok('nothing is left at the back', counts().slice(-1)[0] !== '32' || kinds().slice(-1)[0] !== 'empty',
+   kinds().slice(-1)[0]+'/'+counts().slice(-1)[0]);
 ok('only one of them exists', $$('#segTable .content-note').filter(n=>/the clutch/.test(n.textContent)).length === 1);
-ok('the note follows it', /right/.test($('#clutchNote').textContent), $('#clutchNote').textContent);
+ok('and the choice is remembered', $('#loomClutch').value === 'right');
+ok('the note says the left of the board, whichever end',
+   /left of the board/.test($('#clutchNote').textContent), $('#clutchNote').textContent.slice(0,70));
 
 console.log('\n== the pins are yours to change ==');
 {
-  const at = kinds().length - 1;
-  setVal($$('#segTable input[data-act=count]')[at], '40'); await wait(300);
-  ok('the count can be changed', counts().slice(-1)[0] === '40', counts().slice(-1)[0]);
+  setVal($$('#segTable input[data-act=count]')[0], '40'); await wait(300);
+  ok('the count can be changed', counts()[0] === '40', counts()[0]);
   setSel($('#loomClutch'), 'left'); await wait(350);
-  ok('and a changed count is carried across, not reset', counts()[0] === '40', counts()[0]);
-  setSel($('#loomClutch'), 'right'); await wait(350);
-  setVal($$('#segTable input[data-act=count]').slice(-1)[0], '32'); await wait(300);
+  ok('and a changed count is kept, not reset', counts()[0] === '40', counts()[0]);
+  setVal($$('#segTable input[data-act=count]')[0], '32'); await wait(300);
 }
 
 console.log('\n== it can be taken off ==');
 {
   const before = kinds().length;
-  const del = $$('#segTable button[data-act=del]').slice(-1)[0];
-  click(del); await wait(300);
+  click($$('#segTable button[data-act=del]')[0]); await wait(300);
   ok('the group deletes like any other', kinds().length === before - 1, kinds().length+' of '+before);
   ok('and the note says how to get it back', /removed/.test($('#clutchNote').textContent), $('#clutchNote').textContent);
-  setSel($('#loomClutch'), 'left'); await wait(350);
-  ok('changing the end puts it back', counts()[0] === '32', counts()[0]);
   setSel($('#loomClutch'), 'right'); await wait(350);
+  ok('picking an end puts it back at the left', counts()[0] === '32', counts()[0]);
 }
 
 console.log('\n== and taking it off again ==');
@@ -111,8 +112,10 @@
 }
 
 console.log('\n== it stays put ==');
-// the eight standard groups, with the clutch sitting behind them
-[4,4,500,8,720,500,4,52].forEach((v,i)=>{ const e=$$('#segTable input[data-act=count]')[i]; if(e) setVal(e,String(v)); });
+// the clutch is row 0, so the eight standard groups start at row 1
+ok('the clutch is the first group', kinds()[0] === 'empty' && counts()[0] === '32',
+   kinds()[0]+'/'+counts()[0]);
+[4,4,500,8,720,500,4,52].forEach((v,i)=>{ const e=$$('#segTable input[data-act=count]')[i+1]; if(e) setVal(e,String(v)); });
 setVal($('#totalDeclared'),'1824'); await wait(500);
 ok('they add up with the clutch included', /1,824 of 1,824/.test($('#alloc').textContent.replace(/\s+/g,' ')),
    $('#alloc').textContent.replace(/\s+/g,' ').slice(0,90));
@@ -128,8 +131,12 @@
 ok('the end survives a reload', $('#loomClutch') && $('#loomClutch').value === 'right', ($('#loomClutch')||{}).value);
 ok('the field is still shown', $('#clutchField') && $('#clutchField').hidden === false);
 ok('and the group came back as the clutch', /the clutch/.test($('#segTable').textContent));
-ok('still at the back', $$('#segTable input[data-act=count]').slice(-1)[0].value === '32',
-   $$('#segTable input[data-act=count]').slice(-1)[0].value);
+ok('still the first group, at the left of the board',
+   $$('#segTable input[data-act=count]')[0].value === '32',
+   $$('#segTable input[data-act=count]')[0].value);
+ok('and still marked as the clutch',
+   /the clutch/.test($$('#segTable .seg-row[data-row]')[0].textContent),
+   $$('#segTable .seg-row[data-row]')[0].textContent.replace(/\s+/g,' ').slice(0,70));
 
 console.log('\n== result ==');
 if (errors.length){ console.log('  '+errors.length+' problem(s)'); errors.forEach(e=>console.log('   - '+e)); }
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/ui/19-locks.mjs ../pinmap/tests/ui/19-locks.mjs
--- ../verify/tests/ui/19-locks.mjs	2026-08-03 11:34:51.581800249 +0000
+++ ../pinmap/tests/ui/19-locks.mjs	2026-08-04 05:29:36.661510701 +0000
@@ -90,6 +90,26 @@
   note('loading Google is given a time limit', !/Google did not answer/.test(src));
   note('two cloud starts cannot race', !/if \(cloud\.starting\) return cloud\.starting/.test(src));
 }
+
+console.log('\n== the account is not asked to sign in again every time ==');
+{
+  const src = fs.readFileSync(process.argv[2],'utf8');
+  // Firebase keeps the sign-in in the browser. Nothing used to ask it for one,
+  // so every reload woke with no user and sent the weaver back to Google.
+  note('the kept sign-in is listened for', !/onAuthStateChanged\(cloud\.auth/.test(src));
+  note('and it is kept in this browser on purpose',
+       !/setPersistence\(cloud\.auth, authMod\.browserLocalPersistence\)/.test(src));
+  note('waking looks for that sign-in first', !/restoreCloud\(\)/.test(src));
+  note('and boot no longer goes straight to Google',
+       /state\.account && !cloud\.user\) reconnectCloud\(\)/.test(src));
+  note('Google is only troubled when there is no session',
+       !/if \(ready && cloud\.user\) \{ await syncNow\(\); return true; \}/.test(src));
+  // and the other way about: signing out has to end the Firebase session too
+  note('signing out ends the Firebase session as well',
+       !/cloud\.mod\.signOut\(cloud\.auth\)/.test(src));
+  note('a session that runs out says so rather than going quiet',
+       !/The sign-in has run out/.test(src));
+}
 function note(n,bad,detail){ bad?(errors.push(n),console.log('  FAIL '+n+(detail?'  -> '+detail:''))):console.log('  ok   '+n); }
 
 console.log('\n== result ==');
diff -ru -x node_modules -x .git -x core.mjs ../verify/tests/ui/20-inputs.mjs ../pinmap/tests/ui/20-inputs.mjs
--- ../verify/tests/ui/20-inputs.mjs	2026-08-03 11:34:51.581800249 +0000
+++ ../pinmap/tests/ui/20-inputs.mjs	2026-08-03 18:22:07.589345162 +0000
@@ -119,6 +119,76 @@
   ok('and the right file is simply accepted', /200 × 50/.test(meta()) && !/could not/i.test(meta()), meta());
 }
 
+console.log('\n== an uploaded weave has somewhere to go ==');
+{
+  // A weave in the library is only ever chosen on a Locking group. That was
+  // nowhere on screen, so an uploaded weave looked as though it vanished.
+  click($$('nav.steps button').find(b=>b.dataset.go==='loom')); await wait(400);
+  const note = $('.wl-note');
+  ok('the library says where a weave gets used', note && /Weave library/.test(note.textContent),
+     note ? note.textContent.trim().slice(0,70) : '(no note)');
+
+  // add one, then check it reaches the Locking row's list
+  const wf = $('#weaveFile');
+  const buf = bmp(8,8);
+  const f = new w.File([new Uint8Array(buf)],'twill.bmp',{type:'image/bmp'});
+  const ab = new Uint8Array(buf).buffer.slice(0); f.arrayBuffer = async()=>ab;
+  setVal($('#weaveName'),'Shed twill');
+  Object.defineProperty(wf,'files',{value:[f],configurable:true});
+  click($('#addWeave')); await wait(700);
+
+  ok('it lands in the library', /Shed twill/.test($('#weaveList').textContent));
+
+  // The library is a group of its own now. It used to be a heading part way
+  // down the Locking row's list of satins, where nobody who had just uploaded
+  // a weave would think to look.
+  ok('Weave library is offered as a group',
+     [...$$('#segTable select[data-act=kind]')[0].options].some(o=>/weave library/i.test(o.text)),
+     [...$$('#segTable select[data-act=kind]')[0].options].map(o=>o.text).join('|'));
+  ok('and there is a button to add one', !!$('button[data-add="weaveLib"]'));
+
+  click($('button[data-add="weaveLib"]')); await wait(500);
+  const kinds = $$('#segTable select[data-act=kind]').map(x=>x.value);
+  ok('adding one puts it on the pin map', kinds.includes('weaveLib'), kinds.join(','));
+
+  const picks = $$('#segTable select[data-act=weave]');
+  ok('both woven groups have a picker', picks.length === 2, String(picks.length));
+  ok('Locking offers satins and twills only',
+     /satin or twill/i.test(picks[0].options[0].text) && !/Shed twill/.test(picks[0].textContent),
+     picks[0].options[0].text);
+  ok('Weave library offers the uploaded weaves only',
+     /from the library/i.test(picks[1].options[0].text) && /Shed twill/.test(picks[1].textContent),
+     picks[1].options[0].text);
+
+  {
+    const lib = picks[1];
+    lib.value = [...lib.options].find(x=>/Shed twill/.test(x.text)).value;
+    lib.dispatchEvent(new w.Event('input',{bubbles:true}));
+    await wait(500);
+    const after = $$('#segTable select[data-act=weave]');
+    ok('choosing from the library takes',
+       after[1].options[after[1].selectedIndex].text.includes('Shed twill'),
+       after[1].options[after[1].selectedIndex].text);
+    const at = $$('#segTable select[data-act=kind]').findIndex(x=>x.value==='weaveLib');
+    ok('and the group takes the weave\'s pin count',
+       $$('#segTable input[data-act=count]')[at].value === '8',
+       $$('#segTable input[data-act=count]')[at].value);
+  }
+
+  // take the group away again and the library says where to put one
+  {
+    const at = $$('#segTable select[data-act=kind]').findIndex(x=>x.value==='weaveLib');
+    click($$('#segTable button[data-act=del]')[at]); await wait(500);
+    ok('with no Weave library group, it says to add one',
+       /Nowhere to use/.test($('#weaveList').textContent),
+       $('#weaveList').textContent.replace(/\s+/g,' ').slice(0,90));
+    click($('button[data-add="weaveLib"]')); await wait(500);
+    ok('and the hint goes once there is one again',
+       !/Nowhere to use/.test($('#weaveList').textContent),
+       $('#weaveList').textContent.replace(/\s+/g,' ').slice(0,60));
+  }
+}
+
 console.log('\n== the app never shows its own workings ==');
 {
   const src=fs.readFileSync(process.argv[2],'utf8');
Only in ../pinmap/tests/ui: 21-border-mono.mjs
Only in ../pinmap/tests/ui: 22-board-ends.mjs
Only in ../pinmap/tests/ui: 23-credits.mjs
Only in ../pinmap: tools-codes.mjs
